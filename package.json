{
  "name": "requity-platform",
  "version": "0.1.0",
  "private": true,
  "description": "REQUITY platform: static frontend + secure Vercel API routes over Supabase.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.0"
  }
}
